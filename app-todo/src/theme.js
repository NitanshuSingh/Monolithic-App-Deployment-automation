import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#00f52dff', // Change this to your primary color
    },
    secondary: {
      main: '#00ba44ff', // Change this to your secondary color
    },
  },
});

export default theme;
